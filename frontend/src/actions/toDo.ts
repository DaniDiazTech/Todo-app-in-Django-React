import { toDoActions } from '../types/todoTypes';
const initialState = {}

interface actions {
    add: 
    delete: 
    getOne: 
    list: 
}


export const toDoReducer = (state: typeof initialState, action:actions) =>{
    switch (action){
        case action.add:
            return {

            }
        case action.delete:
            return {

            }
        case action.getOne:
            return {

            }
        case action.list:
            return {

            }
        case action.update:
            return {

            }
    }
}